# =============================================================================
# Legacy .env Override File
# =============================================================================
# This file is loaded LAST and can override any setting from envs/*.
# For new configuration, prefer using the hierarchical envs/ structure:
#   - envs/.env.base     (shared config)
#   - envs/.env.dev      (development - localhost)
#   - envs/.env.prod     (production - Railway)
#   - envs/.env.secrets  (API keys - gitignored)
#
# Set APP_ENV=dev or APP_ENV=prod to control which environment is loaded.
# Default is dev (uses localhost backend).
# =============================================================================

# Uncomment to override APP_ENV (default: dev)
# APP_ENV=prod

# The hierarchical env loader will pick up values from envs/ folder.
# Add any local overrides below this line.

# Optional: Demo mode (default: true) - uses static demo data instead of API calls
# IS_DEMO=true
